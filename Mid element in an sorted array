def find_middle_element(arr):
    if not arr:
        return None  

    mid_index = len(arr) // 2
    return arr[mid_index]

sorted_array = [2, 4, 6, 8, 10, 12, 14]
middle_element = find_middle_element(sorted_array)
print(f"The middle element is: {middle_element}")

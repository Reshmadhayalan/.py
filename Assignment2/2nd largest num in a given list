def second_largest(lst):
    unique_numbers = list(set(lst))  
    if len(unique_numbers) < 2:
        return None  
    unique_numbers.sort()
    return unique_numbers[-2]

numbers = [5, 3, 9, 1, 9, 7]
print("2nd largest number:", second_largest(numbers))
